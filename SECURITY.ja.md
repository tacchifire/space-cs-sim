# セキュリティ方針

*English: [SECURITY.md](SECURITY.md)*

CubeRange は意図的な脆弱性を同梱する。
そのため「壊れて見えるものは何でも報告してほしい」という通常の案内は役に立たない。
この文書は線を正確に引く。

## 意図的な弱点と、本物の脆弱性

**意図的なもの。** どの演習にも、そのために作られたファームウェアイメージの中に、意図して置かれた欠陥がある。
そのイメージは `*-vuln` と名付けられ、弱点は演習の `README.md` と `mitigation.md` に書いてある。
見つけることが目的であって、セキュリティ報告ではない。

現在意図的であり、脆弱性ではないもの。

| 場所 | 内容 |
| --- | --- |
| `CUBERANGE_EPS_REQUIRE_AUTH=0` でビルドした `firmware/apps/eps` | 電源指令が認証されない（EX-B01） |
| `CUBERANGE_COMM_ANTIREPLAY=0` でビルドした `firmware/apps/comm` | テレコマンドをリプレイできる（EX-L01） |
| `CUBERANGE_ADCS_TORQUE_LIMIT=0` でビルドした `firmware/apps/adcs` | トルク指令に範囲検査が無い（EX-A01） |
| `CUBERANGE_OBC_PUS8_LENGTH_CHECK=0` でビルドした `firmware/apps/obc` | PUS 8 の引数コピーに境界が無い（EX-F01） |
| `firmware/apps/obc/src/main.c` の `maintenance_inhibit_fdir` | 特権ハンドラを、削除ではなく無効化した状態でイメージ内に残してある——EX-F01 の標的 |
| `src/cuberange/gs/import_policy.py` の `AcceptAnything` | 地上系が、出所・形式・権限のいずれも検査せずにスケジュールファイルを取り込む（EX-G01） |
| `src/cuberange/gs/import_policy.py` の `IMPORT_KEY` | 計画への署名鍵。設計として平文でコミットしている |
| `firmware/apps/eps/src/main.c` の `POWER_TOKEN` | 設計として、共有秘密を平文でコミットしている |
| 演習シナリオ | 認証なしで待ち受ける。しかも**全インタフェース**で。後述する |

**本物であり、報告に値するもの。** 意図した被害範囲からコードやデータが逃げ出せるもの。

- ホスト側 Python の欠陥のうち、ソケットから届くデータで悪用できるもの。地上局、チャネル、コーデック、supervisor、Renode クライアントが該当する。これらは攻撃者が制御するバイト列を解析するのであって、脆弱であることを意図していない。

- `firmware/common/cuberange_proto.c` の欠陥。共有コーデックであり、対策版を含むすべてのノードにコンパイルされる。`make check` が ASan と UBSan の下で走らせ、さらに**デコーダの契約を検査する**シード付きランダム fuzzer にかける——受理したフレームは宣言長が実長と一致し、FECF の剰余がゼロであり、報告する payload はそのフレームの内側になければならない。この fuzzer はカバレッジ誘導ではない。libFuzzer はこのツールチェーンで使えず、他の fuzzer も入っていない（どちらも実測で確認）。**2026-09-12 まで、この行は「fuzz とサニタイザにかけてある」と書いていた。fuzzer は存在せず、`make check` はサニタイズ無しのビルドを走らせていた。**

- 対策版（`*-hard`）にある、対応する演習が「止まる」と主張しているもの。

- ダウンロードした内容を実行するもの、成果物取得の工程以外でネットワークに到達するもの。

- 寄稿された悪意ある**演習**が、自身のシナリオを超えてホストに影響しうるもの。

**既知であり、報告不要：エミュレータは全インタフェースで待ち受ける。**
この項目には以前「非 loopback へのバインド」が報告対象として書かれていた。
その一方でシナリオはまさにそれをしていた。
実行中に `/proc/net/tcp` を読んで測定した結果、宇宙リンクのソケットターミナルも Renode Monitor も `0.0.0.0` にバインドする。
Renode 1.16.1 にこれを変える手段は無い。
`--port` はアドレスを取らず、`emulation CreateServerSocketTerminal` が取るのはポート、名前、真偽値2つである。

問題になるのは Monitor である。
ポートに到達できる者から任意の Renode コマンドを、認証なしで受け付ける。

2026-09-10 から、レンジ自身がこれを封じ込めている。
Renode の起動はすべて loopback だけを含むネットワーク名前空間の中で行われ、
`RenodeSupervisor` はそうでない場所での起動を拒否する（`src/cuberange/safety/` を参照）。
バインド先は今も `0.0.0.0` だが、そこに到達しうるものが他に存在しない。
`CUBERANGE_ALLOW_UNISOLATED=1` はそれを外す。既定ではなく、判断であるべきものとして用意している。

Renode が広くバインドするという報告は既知である。
CubeRange 自身のホスト側コードがそうしているという報告は既知ではなく、歓迎する。
上記の封じ込めを破る方法——名前空間の外から Renode のソケットに到達する経路や、
検査を通らずに起動する経路——も同様に歓迎する。

## 報告方法

このリポジトリの GitHub の非公開脆弱性報告を使ってほしい（Security → Report a vulnerability）。
修正ができるまで報告が非公開に保たれる。

コミット、再現するコマンド、観測した内容を含めてほしい。
ここでは深刻度の評価より再現手順のほうが重要である。
このプロジェクトの規律はまるごと、主張にはそれを確立するコマンドが付く、という一点にある。

保守の当番に予算は付いていない。
1週間以内の受領確認を見込んでよいが、それ以上のことは best effort でしかない。
それでは足りない状況なら、このプロジェクトに依存しないでほしい。

## 対応バージョン

`main` のみ。
リリースはまだ無く、バックポートもサポート期間も無い。
リリースが始まったら、この節がどれをどれだけ支えるかを書く。
それまでは、どれも支えられていないと考えてほしい。

## 適用範囲

CubeRange はシミュレータである。
実在の宇宙機とも地上局とも無線機とも通信しないし、通信させてはならない。
[SAFE_USE.ja.md](SAFE_USE.ja.md) を見てほしい。
