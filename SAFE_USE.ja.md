# 安全な使い方

*English: [SAFE_USE.md](SAFE_USE.md)*

CubeRange は宇宙機のプロトコルに対する攻撃を教える。
手口は本物である。
標的は本物ではない。
この文書は、それが実際に何を意味するかを書く。

## 何が標的なのか

CubeRange が攻撃するものはすべて合成物であり、自分のマシンの中で動く。

- 宇宙機は Renode の中の、エミュレートされた STM32H753 ノード4つである。
- 「無線リンク」は TCP ソケットである。loopback ではない。後述の規則を見てほしい。
- 「バス」は同一プロセス内の Renode の CAN ハブである。
- 識別子はすべて作り物である。宇宙機 ID が `0x0A9` なのは空いていたからで、実在の APID も仮想チャネル ID も周波数も軌道要素も鍵も無い。
- `POWER_TOKEN` は演習のために無作為に選んだ4バイトで、平文でコミットしてある。

実在の宇宙機に対して較正したもの、そこから導いたもの、そこで試験したものは何一つ無い。

## 規則

**実在するものに向けない。** 実在の地上局にも、実在の衛星にも、実在の無線機にも、飛ぶものとバスを共有する実験室にも向けない。
このツールが話すのは実規格のサブセットであり、だからこそこの規則がある。
組み立てるフレームは、置き場所を間違えれば危険なほど本物に近い。

**実在の素材を持ち込まない。** 実在の APID、宇宙機 ID、周波数、TLE、鍵、エンドポイントを、コードにもテストデータにも演習にも置かない。
演習に現実味が要るなら、作り物を用意して、作り物だと README に書く。

**外へ出ていかないようにする。** このプロジェクト全体で外向きの通信は、固定されたビルド成果物の取得だけである。
それは何かを実行する工程とは別に行われる。
外部に連絡する演習は演習ではない。

**入ってくる側は話が別で、この文書は以前それを間違えていた。**
「シナリオは loopback にバインドする」と書いてあった。
そうではない。
固定バージョンの Renode で、シナリオ実行中に `/proc/net/tcp` を読んで測定した結果、宇宙リンクのソケットターミナルも Renode Monitor も `0.0.0.0` で待ち受ける。
すべてのインタフェースである。

これは設定で変えられない。
Renode 1.16.1 にバインドアドレスの指定は無い。
`--port` にも無いし、`emulation CreateServerSocketTerminal` にも無い。
そのシグネチャは `(Int32 port, String name, Boolean telnetMode, Boolean flushOnConnect)` である。

したがって封じ込めはホスト側の仕事になる——そして 2026-09-10 から、レンジ自身がそれを持つようになった。
「安全な場所で動かすこと」は助言であり、助言は制御ではないからである。

Renode の起動はすべて、loopback だけを含むネットワーク名前空間の中で行われる。
Renode は今も `0.0.0.0` にバインドするが、そこに他のインタフェースが存在しない。
`make` は自動でそうする。そして `RenodeSupervisor` は、そうでない場所では**起動を拒否する**。
祈りながら起動するのではなく。

```
python3 -m cuberange.safety.isolate --probe        # このホストで何が使えるか、使えないなら理由
python3 -m cuberange.safety.isolate -- <コマンド>   # 名前空間の中で何かを走らせる
```

機構は 2 つ試すが、選択は「どのバイナリが存在するか」ではなく**実際に走らせて結果を見て**決める。
この違いは机上のものではない。現行の Ubuntu では `unshare` はインストールされているのにポリシーで拒否される
（`kernel.apparmor_restrict_unprivileged_userns=1`）。バイナリの有無で選んでいたら、それを選んでいた。
`bwrap`（bubblewrap）は root もポリシー変更も要らず、そこで動くのはこちらである。

どちらも動かないホストではレンジは起動しない。それが意図した挙動である。
それでも進めるなら `CUBERANGE_ALLOW_UNISOLATED=1` を設定する——意識的に、
受信ポートをファイアウォールで塞いだマシンか、完全に信頼できるネットワークで。
共有ネットワークや公開ネットワークでは動かさない。踏み台になる位置にも置かない。
そこで露出するのは Monitor であり、任意の Renode コマンドを認証なしで受け付ける。
エミュレータの制御であり、Renode 自身が持つ範囲でそれが動くマシンの制御でもある。

EX-L01 で攻撃者が taping するホスト側プロキシ `LinkChannel` は、既定で `127.0.0.1` にバインドする。
そこは正確だった。
loopback でないのはエミュレータ自身の待ち受けである。

**実システムに関する主張の根拠に使わない。** [ASSURANCE.ja.md](ASSURANCE.ja.md) を見てほしい。
Renode で exploit が通ることは、コードについて何かを語る。
試験していない宇宙機については何も語らない。

## 教材として使う場合

参加者が攻撃しているのはシミュレーションだと、はっきり言うこと。
部屋の中では自明に聞こえる。
半年後の報告書では自明でなくなる。
「衛星を侵害した」という一文はそこから生まれる。

攻撃には必ず対策を対にすること。
どの演習も対策を同梱しており、その対策が**解決しないこと**の記述も付いている。
売り込みすぎた対策は、対策が無いより悪い。

## 寄稿する場合

[CONTRIBUTING.ja.md](CONTRIBUTING.ja.md) を見てほしい。
新しい演習には、合成の標的、対策、両方向のテスト、そして外部通信をしないことが要る。
