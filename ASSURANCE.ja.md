# CubeRange の結果が支持するもの、しないもの

*English: [ASSURANCE.md](ASSURANCE.md)*

訓練に有用なシミュレータは、飛行ハードウェアに関する証拠と取り違えられる。
たいていは悪意なく、書き手が急いでいるときに。
この文書は、その取り違えを意図的にしなければできないようにするために置いてある。

## ここでの結果が支持すること

- **あるコード上の書き方が悪用可能であること。** ファームウェアは実物の Zephyr であり、実物のツールチェーンでクロスコンパイルされ、Cortex-M7 のモデル上で命令として動く。攻撃が通ったとき、バグはコードにある。台本の側ではない。

- **あるプロトコルの挙動が規格の言うとおりであること。** ただし設計仕様書に書かれた範囲までで、それ以上ではない。層ごとに事情が違うので、正確に書く。

  | 層 | 独立オラクル | 場所 |
  | --- | --- | --- |
  | Space Packet の一次ヘッダ | spacepackets | `tests/pytest/test_oracle_spacepacket.py` |
  | FECF の CRC-16 | crcmod と CCSDS 132.0-B-3 の本文 | `tests/pytest/test_crc.py` |
  | CSP v1 と CFP over CAN | libcsp 自身を動かして生成 | `tests/golden/csp.json` |
  | **PUS-C の副ヘッダ** | **無し** | — |
  | **TM/TC 転送フレーム** | **無し** | — |

  下2行は、このプロジェクト自身の2つ目の実装としか突き合わせていない。
  設計仕様書の 9.3 節が冒頭で「それは証明にならない」と書いているものである。
  PUS はさらに厳しい。
  一次資料が入手できなかった層でもあり、足場が最も薄い。
  `tools/gen_golden.py` は両方を独立オラクルから生成できるが、ベクタはまだコミットされておらず、消費するテストも無い。

- **ある対策が特定の攻撃を止め、かつ守るべき機能を壊さないこと。** どの演習も両方を assert する。
  加えて、正規操作が成功する場合も assert する。

- **ある制御を作る価値があること。** それが演習環境の目的である。

## 支持しないこと

CubeRange の結果を、次のいずれかの証拠として引用してはいけない。

| 主張 | 理由 |
| --- | --- |
| 飛行適合性、認定 | 認定プロセスに近づいたものは何一つない |
| 実時間挙動 | Renode は機能エミュレータである。命令タイミング、キャッシュ、バス競合、割り込み遅延はモデル化されていない |
| RF やリンク層の適合 | 外側のフレーミングは「CubeRange lab framing」であって CCSDS の CLTU ではない。変調も符号化もドップラーも雑音も無い |
| CCSDS や ECSS への適合 | 実装しているのはサブセットであり、逸脱は文書化してある。適合とは試験制度のことで、このプロジェクトはそれを受けていない |
| 暗号の保証 | SDLS は未実装である。実装したとしても、それは検証済み実装ではなく実験室実装になる |
| ハードウェアセキュリティ | サイドチャネルもグリッチもフォールト注入もシリコンエラッタも無い。Renode がモデル化するのはレジスタであって物理ではない |
| 放射線耐性、故障耐性 | 一切モデル化していない |
| 実在の衛星が脆弱である、あるいは脆弱でないこと | ここにあるのは合成標的である。実システムに似ているのは同じ規格を実装しているからで、実システムであることとは違う |

## 実測で分かった忠実度の限界

いずれもエミュレータ自体を試験して出てきたもので、それぞれが演習の一分類を不可能にする。

- **Renode は未マップ番地へのアクセスでフォールトを起こさない。** 野良ポインタを起点にする演習は静かに何も起きない。だから作っていない。

- **`MPU_CTRL.PRIVDEFENA` が特権アクセスで無視される。** 強制されるのは、明示的にプログラムした MPU 領域だけである。「領域が無いなら拒否される」はここでは成り立たない。シリコン上とは違う。

- **CAN ハブはバス ACK をモデル化しない。** エラーフレームや bus-off の診断は、このプラットフォームでは教えられない。

- **Renode は MPU の execute-never を強制する。** `probe.sh` の section G が再現手段である。
  0x24003000 への命令フェッチは拒否され、フラッシュへの同じ問い合わせは成功する。
  EX-F01 がシェルコード注入ではなくコード再利用なのはこのためである。
  シェルコードはここでも実シリコンと同じように失敗する。
  MPU を無効にしないと成立しない演習は、真でないことを教えることになる。

  **問い方が結果を変える。** 1.16.1 の `cpu TranslateAddress` はアクセス種別ではなくアドレスでキャッシュする。
  同じアドレスに対して Read を問い合わせてから InstructionFetch を問い合わせると、両方が成功を返す。
  execute-never をその順序で、つまり自然な順序で確認すると、SRAM が実行可能だという結果になる。
  フェッチを先に問うか、新しいプロセスで問うこと。
  D22 として否定アサーションに固定してあるので、将来の Renode がキャッシュを直せば、この段落の意味が静かに変わるのではなくプローブが落ちる。
