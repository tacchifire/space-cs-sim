# CubeRange の結果が支持するもの、しないもの

*English: [ASSURANCE.md](ASSURANCE.md)*

訓練に有用なシミュレータは、飛行ハードウェアに関する証拠と取り違えられる。
たいていは悪意なく、書き手が急いでいるときに。
この文書は、その取り違えを意図的にしなければできないようにするために置いてある。

## ここでの結果が支持すること

- **あるコード上の書き方が悪用可能であること。** ファームウェアは実物の Zephyr であり、実物のツールチェーンでクロスコンパイルされ、Cortex-M7 のモデル上で命令として動く。攻撃が通ったとき、バグはコードにある。台本の側ではない。

- **あるプロトコルの挙動が規格の言うとおりであること。** ただし設計仕様書に書かれた範囲までで、それ以上ではない。層ごとに事情が違うので、正確に書く。

  | 層 | 独立オラクル | 場所 |
  | --- | --- | --- |
  | Space Packet の一次ヘッダ | spacepackets、ccsdspy、および 133.0-B-2 の表からの手作業デコード | `test_golden_spacepacket.py` |
  | FECF の CRC-16 | crcmod、fastcrc、crc、NASA CryptoLib、および CCSDS 132.0-B-3 の本文 | `test_golden_frame.py` |
  | CSP v1 と CFP over CAN | libcsp 自身を動かして生成 | `tests/golden/csp.json` |
  | PUS-C の副ヘッダ | spacepackets | `test_oracle_pus.py` |
  | TC 転送フレームの一次ヘッダ | NASA CryptoLib（再実装ではなく、octets を解析させる）と 232.0-B-4 からの手組み | `test_golden_transfer_frame.py` |
  | TM 転送フレームの一次ヘッダ | `spacepackets.ccsds.tm_frame` | `test_golden_transfer_frame.py` |
  | SDLS の TC セキュリティヘッダ／トレーラ | NASA CryptoLib（自分が作っていないフレームを解析させる）、MAC は libsodium と NIST の AES-256-GCM ベクタ | `test_golden_sdls.py` |

  下 2 行は 2026-09-10 まで**「無し」**だった。そしてその上の行は読み違えやすかった——
  転送フレームを**覆う** FECF には独立した意見が 4 つあるのに、
  **ヘッダのフィールド配置**には 1 つも無かった。
  ヘッダ内でフィールドが入れ替わっていても、このツリーのすべてのテストが通る状態だった。

  新しい 2 行が何を示し、何を示さないか:

  - TC: バージョン、bypass、制御指令、spare、SCID、VCID、フレーム長、フレーム順序番号のすべてを、
    このプロジェクトが組んだのではない octets から CryptoLib が復元した。0 だけでなく各フィールドの
    最大値でも確認している。CryptoLib は `フレーム長フィールド + 1 == octets 数` も強制するので、
    ここで最も間違えやすいフィールド——自前のデコーダとは完璧に往復するのに実機の受信側には
    ことごとく拒否される種類の誤り——が外部から確認されたことになる。
  - TM: 6 オクテットの一次ヘッダを `spacepackets` が名前付きフィールドから組み、オクテット単位で比較する。
    **ただし Transfer Frame Data Field Status は 1 つの状態でしか検査していない。**
    このプロジェクトは first header pointer を常に 0、全フラグを 0 で出すため、
    その他の値についてベクタは何も述べておらず、そこの誤りは何も捕まえない。
  - どちらも TC のセグメントヘッダや TM の副ヘッダは覆わない。このプロジェクトはどちらも出さない。
  - フィールドの**幅**を守っているのはマスクではなく範囲検査であり、テストが検査しているのもそちらである。
    TM の VCID マスクを 3 ビットから 6 ビットに広げても、コミット済みのどのベクタも変わらない。
    7 を超える VCID はマスクに届く前に拒否されるからである。
    これは実測した——最初に書いたテストは逆のことを主張していた。

- **ある対策が特定の攻撃を止め、かつ守るべき機能を壊さないこと。** どの演習も両方を assert する。
  加えて、正規操作が成功する場合も assert する。

- **ある制御を作る価値があること。** それが演習環境の目的である。

## 支持しないこと

CubeRange の結果を、次のいずれかの証拠として引用してはいけない。

| 主張 | 理由 |
| --- | --- |
| 飛行適合性、認定 | 認定プロセスに近づいたものは何一つない |
| 実時間挙動 | Renode は機能エミュレータである。命令タイミング、キャッシュ、バス競合、割り込み遅延はモデル化されていない |
| RF やリンク層の適合 | 外側のフレーミングは「CubeRange lab framing」であって CCSDS の CLTU ではない。変調も符号化もドップラーも雑音も無い |
| CCSDS や ECSS への適合 | 実装しているのはサブセットであり、逸脱は文書化してある。適合とは試験制度のことで、このプロジェクトはそれを受けていない |
| 暗号の保証 | SDLS の**認証**サービスは 2026-09-12 にホスト側で実装した——`src/cuberange/proto/sdls.py`、OpenSSL 経由の AES-256-GCM、libsodium と NIST ベクタで照合済み。**宇宙機はまだ走らせていない**。鍵管理は無く、鍵は 1 本を焼き込む。実験室実装であり、この行が最初からそう書いてきたとおりのものである |
| ハードウェアセキュリティ | サイドチャネルもグリッチもフォールト注入もシリコンエラッタも無い。Renode がモデル化するのはレジスタであって物理ではない |
| 放射線耐性、故障耐性 | 一切モデル化していない |
| 実在の衛星が脆弱である、あるいは脆弱でないこと | ここにあるのは合成標的である。実システムに似ているのは同じ規格を実装しているからで、実システムであることとは違う |

## 実測で分かった忠実度の限界

いずれもエミュレータ自体を試験して出てきたもので、それぞれが演習の一分類を不可能にする。

- **Renode は未マップ番地へのアクセスでフォールトを起こさない。** 野良ポインタを起点にする演習は静かに何も起きない。だから作っていない。

- **`MPU_CTRL.PRIVDEFENA` が特権アクセスで無視される。** 強制されるのは、明示的にプログラムした MPU 領域だけである。「領域が無いなら拒否される」はここでは成り立たない。シリコン上とは違う。

- **CAN ハブはバス ACK をモデル化しない。** エラーフレームや bus-off の診断は、このプラットフォームでは教えられない。

- **Renode は MPU の execute-never を強制する。** `probe.sh` の section G が再現手段である。
  0x24003000 への命令フェッチは拒否され、フラッシュへの同じ問い合わせは成功する。
  EX-F01 がシェルコード注入ではなくコード再利用なのはこのためである。
  シェルコードはここでも実シリコンと同じように失敗する。
  MPU を無効にしないと成立しない演習は、真でないことを教えることになる。

  **問い方が結果を変える。** 1.16.1 の `cpu TranslateAddress` はアクセス種別ではなくアドレスでキャッシュする。
  同じアドレスに対して Read を問い合わせてから InstructionFetch を問い合わせると、両方が成功を返す。
  execute-never をその順序で、つまり自然な順序で確認すると、SRAM が実行可能だという結果になる。
  フェッチを先に問うか、新しいプロセスで問うこと。
  D22 として否定アサーションに固定してあるので、将来の Renode がキャッシュを直せば、この段落の意味が静かに変わるのではなくプローブが落ちる。

エミュレータとライブラリの欠陥の完全な登録簿は
`docs/superpowers/specs/2026-08-28-cuberange-design.md` の 3.2 節にある。

## 主張の出どころ

設計が依拠する能力は、すべて `tools/renode-probe/probe.sh` が再現する。これは `make check` の
最初のジョブとして走る（`check: probe`）。そこで再現できない主張は設計に載せない。
プローブ自身も、わざと失敗させる自己テストを内蔵している。失敗しえないゲートはゲートではない。
