# CubeRange — 作業ノート

このリポジトリで何かをする前に読むこと。英語版は [CLAUDE.md](CLAUDE.md)。

## これは何か

宇宙サイバーセキュリティの実習レンジである。衛星ファームウェアは Renode の中を命令単位で走る。
CAN バス上のエミュレートされた STM32H753 ノード群、ホスト上の地上局、そして双方向に検証される
セキュリティ演習——攻撃が通ること、そして対策がそれを止めること、その両方を確かめる。

宇宙機は 2 機、各 4 ノード、これらは 1 つのエミュレーションで同時に走る。同一性はビルドパラメータで、
`firmware/common/identity.cmake` が `CUBERANGE_SAT_INDEX` から CSP アドレスと SCID を導出する。
だからソースツリーは 1 つのまま、どの宇宙機のどのノードも作れる。衛星 0 は既存のシナリオと解説が
名指ししている番号を保つ。**振り直してはいけない。**

**CI が通るところを観測した。実行 8、コミット `eaf07ce`、main、両ジョブとも成功、
ゲートは ubuntu-latest 上で 838 秒かけて `CHECK PASSED` に到達した。**
この一文が「誰も走っているところを見ていない」を置き換えたのは 2026-09-10 で、
それより 1 日も早くはない。その日まで真ではなかったからである。

しかも意図していたより強い意味で真だった。API が返したのは「実行 0 回」ではなく
**「ワークフロー 0 個」**だった。GitHub がワークフローを登録するのは最初に実行されたときで、
`push` は main にしか一致せず、main に `.github` は無く、`workflow_dispatch` は既定ブランチ上の
ワークフローにしか現れない。ファイルは push され、3 つの文書で説明され、そして不活性だった。
押されていないボタンではなく、ボタンが無かった。

そこから緑にするまでに 3 つの欠陥を要した。どれもこのマシンでは出ない。
ここでは環境が常に既に入っているからである。

1. 作業ブランチが push トリガに入っておらず、そもそも何も走らなかった;
2. キャッシュは `~/zephyrproject` と `~/zephyr-sdk` を復元するが、それらを駆動する pip
   パッケージは復元しない。そして `setup-toolchain.sh` は**キャッシュミスのときしか走らない**。
   空の実行は通り、温まった実行はすべて `west build` で死んだ。入力だけ復元して道具を復元しない
   キャッシュは 2 回目に失敗し、**2 回目以降はずっと 2 回目**である;
3. `west` だけ直したら Zephyr 自身の `requirements.txt` が残り、同じ失敗が戻ってきた。
   ダウンロードをキャッシュし、環境は毎回入れる。

緑が 1 回出たことは、信頼できることを意味しない。そして最も長く抱えた仮説は**外れていた**。
probe.sh の「4 ノード 1.5x」という速度下限——この 8 コア機は 4.3〜5.1x で越える——は、
2 vCPU のランナーでは真っ先に疑うべきものに見えた。実際にはそこでも 110 秒で通る。
ホストのせいにするのは楽な推測で、そして誤りだった。

現在 6 つの演習が動く。うち 5 つは攻撃起点ごとに 1 つずつ——EX-B01（機内バス）、
EX-L01（宇宙リンク）、EX-A01（ADCS の指令範囲）、EX-F01（OBC 自身の PUS 8 パーサ）、
EX-G01（何を送るかを決める地上系）。EX-G02 は 2 つ目の地上系演習で、新しい起点は持たない——
その主題は**起点が正当である**ことである。実在の局が、権限の無い実在の指令を送る。
正規のフレームとは 1 オクテットしか違わない。アサーションは 22、すべて実測である。

EX-G01 はホスト側なので、「1 フラグだけ違う」ことの証明は別の形をとる。`Scheduler` は 1 つ、
`ImportPolicy` は 2 つ。そして `schedule.py` がどちらかのポリシークラスの名前を書いただけで
`test_schedule_policy.py` が落ちる。自分がどちらのポリシーを持っているか調べられるスケジューラは、
ほかの何においても違いうるからである。

設計は `docs/superpowers/specs/2026-08-28-cuberange-design.md`。その 16 節が、誤りだと判明した
主張の記録である。

## 規則

**走らせていない主張を書かないこと。**

これは標語ではなく、このプロジェクトの形そのものの理由である。設計の初版は「検証済み」と称する
Renode 能力の節を丸ごと主張していたが、一度も実行されていなかった——Renode はインストールすら
されていなかった。実行してみると 6 つが反証され、その中には MCU の選定も含まれていた。
2 版の中心的な性能前提は 8 倍外れていた。どちらも 16 節に記録してある。記録の有用さは、
みっともなさの代償より大きい。

具体的には:

- 設計が依拠する Renode 能力は、すべて `tools/renode-probe/probe.sh` が再現する。
  主張を足すならプローブも足す。プローブが再現できないなら、その主張を消す。
- **ハーネスは失敗できなければならない。** `probe.sh` の section Z は既知の不正入力を自分に食わせ、
  それが検出されることを要求する。以前の版は、出力ディレクトリが無いときに 34 プローブすべてを
  PASS と報告していた。存在しないファイルへの `grep` はエラーを見つけないからである。
- `make check` は `CHECK PASSED` で終わる。その行が見えないなら、通っていない——
  8500 行のビルド出力の中に落ちたサブターゲットが隠れ、要約行が「passed」と言っていたことがある。
- 読むより測る。ここに書いてあることの大半は、Renode を走らせて先に間違えたことで分かった。

## コマンド

```bash
make out          # このチェックアウトのビルドディレクトリ。以下のパスはすべてこの下にある
python3 -m cuberange.safety.isolate --probe   # このホストで使えるネットワーク名前空間の機構
make probe        # Renode 能力チェック 40 件。うち 5 件はわざと失敗させる自己テスト   約 6 分
make firmware-f01 # 衛星 0 の全イメージ: COMM, OBC（両方）, EPS（両方）, ADCS（両方）
make firmware-sat1 # 同じ 4 役を宇宙機 1 として
make firmware-g01 # EX-G01 が相手にする硬化版 OBC と EPS
make constellation # 8 ノード、2 宇宙機、そして両者のバス隔離
make golden       # 独立オラクルを再ビルドし tests/golden を再生成（ネットワークが要る）
tools/ci.sh       # CI が走らせるもの: 環境チェック、そして make check
tools/ci.sh --self-test   # 環境チェックが失敗できることを示す
make demo-p0      # PUS 17 の往復。地上局から OBC へ、そして戻り
make determinism  # 規則 G1/G2: CI プロファイルで 3 回走らせ、UART 捕捉がバイト一致    約 30 秒
make pair-gate    # 脆弱版／緩和版の各ペアが、ちょうど 1 つのビルドフラグだけ違うこと
make verify-all   # 全演習、各 3 アサーション
make check        # 以上すべて                                                        約 11 分
make soak-p0      # ウォッチドッグ下で 30 回連続の往復                                 約 4 分
```

`make probe` はホストの混雑に非常に敏感である。空いていれば約 6 分、Renode の別作業が 2 つ
同居していたときは 35 分かかった。負荷を確かめずに、遅いプローブを退行と読まないこと。

ツールチェーンの導入は `./tools/setup-toolchain.sh`。冒頭のコメントを読むこと。
実際に時間を失った行き止まりが 2 つ記録してある。

## 固定してあるもの、その理由

どれも、変えると何かが具体的に壊れる。

| 固定 | 理由 |
| --- | --- |
| Renode **1.16.1**、ビルド `d66b0c2a-202602160923` | `probe.sh` が両方を検査する。`master` にある機能（RAMN ボード、STM32L5 の FDCAN）は使えない |
| Zephyr **v4.1.0** | `main` は Python 3.12 以上を要求し、3.10 では cmake の時点で落ちる。`cmake/modules/python.cmake` はタグごとに main 3.12 / v4.1.0 3.10 / v3.7.0 3.8 |
| `csp_conf.version = 1` を `csp_init()` の前に設定 | libcsp の既定は **2** で、ヘッダと CFP の配置を**実行時**に選ぶ。v2 同士のノードは完全に健全に見え、v1 のツールだけが無言で無視される |
| `SetGlobalQuantum "0.002"` | ファームウェア出力がバイト一致する最大の量子。これより大きいと、タイミングが無言で、しかも実行時間に依存してずれる——5 ms は 5 秒でも 10 秒でも正しく見え、20 秒で壊れる |
| `SetGlobalAdvanceImmediately true`（interactive のみ） | Renode の実時間スロットルを外す。無いと 4 ノードは 2.34x ではなく 0.301x で走る。CI ではこれを使わず、代わりに `SetGlobalSerialExecution` と `SetSeed` を足す |
| CSP アドレスは **5 ビット** | `csp.py` は 0x1F でマスクし、CFP の CAN 識別子も同じ幅を使う。宇宙機あたり 8 アドレスなら 4 機まで。`identity.cmake` はそれを超える添字を、他機のノードに巻き戻すのではなく拒否する |
| CSP ポートは **10**、17 ではない | libcsp の `CSP_PORT_MAX_BIND` の既定は 16 で、それより上は一時的な送信元ポート用に予約されている |

## エラーメッセージを出さない失敗モード

この領域はこれで満ちている。以下はすべて、ここで実際に踏んだものである:

- **`CANMessageFrame(id, data)` は標準 11 ビットフレームを作る。** 拡張 29 ビット ID で
  フィルタしている受信側はこれを落とす。フレームの中身は完全に正しく、そして見えない。
  `extendedFormat: true` を明示すること。
- **libcsp のバージョン不一致** — 固定表を見よ。どこにもエラーは出ない。
- **`csp_sendto` はコネクションレス**で、`csp_bind`/`listen`/`accept` を使うソケットには決して届かない。
  `csp_connect`/`csp_send` を使うこと。
- **Monitor のバッチは最初に失敗したコマンドで無言で中断し**、以降の応答がすべてずれる。
  返ってきたフィールド数を数えること。
- **Monitor は指数表記を壊す**: `1e3` は 1 に、`0b101` は 0 になる。Python の repr は小さい float を
  `1e-05` と出す。固定小数で整形すること。
- **Monitor の読み出しは周辺モデルを経由する。** read-to-clear レジスタをポーリングすると
  ファームウェアの状態が壊れる。GPIO の ODR、`LED.State`、`GetGPIOs` は安全。それ以外は仮定しないこと。
- **`Sensors.OB1203` は構築時にプロセス全体を落とす。** `Sensors.VEML7700` は 1.16.1 に存在しない。
  どちらも否定アサーションとして固定してある。将来これらが直った版が出れば CI が落ちる。
- **`gpioPortB` のピン 0 は `invertedAFPins` に入っている。** そこに置いた電源線は ODR と逆に読める。
  ポート D は綺麗で、だから COMM の電源線は `gpioPortD` のピン 5 にある。断定する前に極性を測ること。
- **Zephyr の STM32 GPIO ドライバは BSRR ではなく ODR でトグルする。** BSRR のウォッチポイントは
  すべてのトグルを取り逃がす。
- **`--hide-log` はインジェクタ自身の診断も消す。** 演習はこれを付けずに走らせる。
  Renode のログが証拠である。
- **Renode の Monitor はプロンプトとコマンドのエコーを別チャンクで出す。** 「プロンプトが来たら」で
  待つクライアントは結果が届く前に戻り、その結果を次のコマンドの出力の先頭で見つけることになる。
  `src/cuberange/renode/monitor.py` はエコーを基準にしている。
- **どの演習にも `solve.py` があり、`sys.path.insert` は最初のものを勝たせる。** EX-G01 の検証は
  無言で EX-F01 のモジュールを受け取り、収集時に「cannot import name PLUGIN_FILE from solve」で
  死んだ。CONTRIBUTING.md は `verify_*.py` の基底名について警告していたが、これは全演習が持つ
  ファイルでの同じ危険である。隣の `solve.py` はパスで読み込むこと。
- **CAN ハブの名前はエミュレーション全体のスコープを持つ。** どちらも `canHub` と書いた 2 つの
  シナリオは、全マシンを 1 本のバスに繋ぐ。何も報告されず、症状は「一方の宇宙機への攻撃が
  もう一方に届く」である。`tests/e2e/test_constellation.py` は隔離を**陽性対照つきで**検査する——
  本来届くべきハブに同じコマンドを送れば通ること、も確かめる。「何も起きなかった」だけの検査は、
  打ち間違いに対しても同じように通ってしまうからである。
- **ノードの再読み込みには全体 `pause` が避けられない。そして他マシンにとっての代償は、
  彼らには観測できない。** 実測: `sysbus LoadELF` は、対象 CPU を停止させてあっても、走行中の
  エミュレーションでは失敗する。一方 `machine RequestReset` と `cpu IsHalted false` は
  pause 無しで成功する。`pause` は**時間領域全体**を止めるので、その間、他の宇宙機の命令カウンタも
  経過仮想時間も凍り、そして両方とも再開する。代償は実時間であり、それが問題になるのは
  実時間の予算を跨いで持っているホスト側コードだけである。
- **`cpu TranslateAddress` はアクセス種別ではなくアドレスでキャッシュする。** あるアドレスについて
  `Read` を問い合わせると、直後の同じアドレスへの `InstructionFetch` が偽の成功を返す。
  別のページやフラッシュは影響を受けない。これが問題なのは、「SRAM は読めるが実行できない」を
  確かめる自然な順序がまさにその順序であり、その順序ではシェルコードが動くという結果になるからである。
  `InstructionFetch` を先に問うか、新しいプロセスで問うこと。probe.sh の section G に
  否定アサーション（D22）として固定してある。
- **失敗したコマンドは `-e` チェーンの残りを中断する。末尾の `quit` も含めて。** Renode はその後
  終了せず、スーパーバイザのウォッチドッグに殺されるまでぶら下がる。そしてログは**空**である。
  出力がプロセスグループとともに失われるからだ。ログの中のエラー文字列を探すハーネスは何も見つけず、
  何も起きなかったと結論する。シナリオが実行プロファイルを**最初の**コマンドとして include するのは
  このためである。パスが間違っていれば、量子を設定しないまま全ノードを走らせるのではなく、
  マシンごと道連れに落ちる。
- **`$OUT` の既定は、かつてどのチェックアウトでも `/tmp/cuberange` だった。つまり同一マシン上の
  2 つの作業コピーが同じディレクトリにビルドしていた。** 双方が `west build -p always` を実行し、
  後に終わった方が `build-obc/` を所有する。何も報告されない。ペアゲートはそれから、一方の
  チェックアウトの脆弱版ともう一方の緩和版を比較し、`expected exactly
  CUBERANGE_OBC_PUS8_LENGTH_CHECK to differ, but the differing cache variables are
  ['CUBERANGE_APID', ...]` と報告した——**このリポジトリのどのファイルにも存在しないフラグ名**を
  名指しして。既定は現在チェックアウトごとに一意である（`src/cuberange/paths.py`。Makefile 側の
  同じ導出と `test_paths.py` が突き合わせる）。そしてゲートは `CMAKE_HOME_DIRECTORY` を最初に読むので、
  メッセージが「これは誰の成果物か」を言う。`make out` がそのディレクトリを表示する。
- **シナリオ側にも同じパスが直書きされており、演習は「試験対象のイメージ」しか指定していなかった。**
  つまり EX-F01 は自分の OBC をこのチェックアウトから読み、その周りの COMM と EPS は
  共有ディレクトリにあったもの——つまり他人のビルド——から読んでいた。現在はすべての `.resc` が
  ファームウェアと捕捉ファイルのパスを `$out` から導出し、`$out` には**既定値が無い**。
  未設定なら Renode は `No such variable: $out` で止まる。他人のビルドで起動するより止まる方がよい。
  Renode 1.16.1 が変数を連結できること（`$comm?=$out/build-comm/zephyr/zephyr.elf` が解決すること）は
  実測した。仮定ではない。起動側は `-e "$out=@<OUT>"` を渡す。シナリオがパスを直書きしたり、
  起動側が渡し忘れたりすれば `test_paths.py` が落ちる。
- **Renode は `0.0.0.0` にバインドし、1.16.1 では変更する手段が無い。** そのため起動はすべて
  `cuberange.safety.isolate` 経由で loopback だけの名前空間に入り、`RenodeSupervisor` はその外での
  起動を拒否する。`make` は自動でそうする。`python3 exercises/.../verify_*.py` を直接叩くと拒否され、
  使うべきコマンドが表示される。バックエンドは `shutil.which` ではなく**実際に走らせて**選ぶ。
  このホストでは `unshare` はインストール済みなのに AppArmor
  （`kernel.apparmor_restrict_unprivileged_userns=1`）に拒否され、動くのは `bwrap` の方だからである。
  隔離下での実測: probe 40/0/3、demo-p0、生 CAN インジェクタを使う EX-B01。
- **少なく数える監視は決して発火せず、「一度も引っかからない監視」と見分けが付かない。**
  supervisor の RSS 集計はプロセス木を 1 段しか降りていなかった。根拠は「Renode の実作業は
  ランチャの子で起きる」というコメントだけだった。実測すると、1.16.1 portable-dotnet の
  `./renode` は子を持たない単一のネイティブプロセスで、1 段でも全木でも 292 MB。
  つまり制限は一度も効いていなかった。Renode が再びラッパとして梱包された瞬間に効き始める。
  現在は全木を歩き、`test_supervisor.py` が孫プロセスに 80 MB を持たせて検査する。
- **誰も読まない状態は、どれほど丁寧に設定されていても飾りである。**
  `PowerDomain.latched_unpowered_at_start` は `state is False` では表現できない区別——
  最初に見た時点で既に落ちていた電源線。halt したマシンも復旧すべきものも無い——を記録していたが、
  呼び出し側の誰も参照していなかった。現在は `outage_applied()` が消費者である。
  `test_powerdomain.py` はこのクラス初のユニットテストでもある。CPU を停止し ELF を再読み込みする
  部品でありながら、全経路が演習の中でしか走っておらず、ここの誤りはファームウェアの不具合に見えていた。
- **変異テストが、黙って「元のコード」を検査していることがある。** Python は `__pycache__` を
  (mtime, size) で有効性判定する。サイズの変わらない 1 文字の変異——`vcid_bits=3` を `=6` に、
  `0x7` を `0x3F` に——を入れ、同じ秒のうちに元へ戻すと、古い `.pyc` が有効に見えたまま残り、
  実行されるのは「戻した後のコード」になる。ここでは実際に、ある変異が「検出されない」と報告され、
  その結論を docstring に書いた後でキャッシュを消したら**答えが違っていた**。
  変異と実行の間、そして復元と次の実行の間で `__pycache__` を消すこと。
- **Renode はある条件下で約 13% の起動で固まり**、10 分で RSS が 17 GB まで膨らむ。
  必ず `src/cuberange/renode/supervisor.py` 経由で起動すること。静かなホストでの監督付き 30 回では
  一度も再現しなかったので混雑が原因らしいが、原因は分かっていない。

完全な登録簿は設計の 3.2 節である。

## 演習

各 5 ファイル。`verify_ex_*.py` は**3 つ**を検査しなければならない: 攻撃が通ること、対策がそれを
止めること、そして**対策版が本来の仕事を今もこなすこと**。3 つ目が無ければ、機能を壊しただけの
対策も通ってしまう。

脆弱版と緩和版のファームウェアのペアは、ちょうど 1 つのビルドフラグだけが違う。かつてこれは
「貢献者が忘れずに打つよう頼まれた `diff`」で証明されていた。現在は `tools/config_diff_gate.py` が
`firmware-matrix.yml` に宣言された全ペアについて証明し、`make check` の一部になっている。
検査は 6 つある。最初の 1 つは「そもそもこの成果物が我々のものか」を確かめるもので、
最後の 3 つは、検査 2 と 3 が通ってもフラグが何もしていない場合がありうるために存在する:

0. 両方が**このソースツリー**から作られたこと。`CMakeCache.txt` の `CMAKE_HOME_DIRECTORY` を読む。
   最初に検査するのは、そうでなければ以降のメッセージがすべて誤導になるからである——
   上の失敗モードの `$OUT` の項を見よ;
1. 2 つの Kconfig 出力が同一であること（836 シンボル、実測）;
2. 他のどの `CUBERANGE_*` キャッシュ変数も違わないこと;
3. 2 つの ELF がバイト同一で**ない**こと——綴りを間違えたフラグは同じイメージを 2 回コンパイルし、
   一度も組み込まれなかった対策を「証明」してしまう;
4. そのフラグがアプリケーションのソースから実際に参照されていること;
5. すべての翻訳単位が、宣言された `-D` 以外は同一のコマンドラインでコンパイルされていること。
   コンパイラオプションで脆弱性を作り出すことを止めるのがこれである。Kconfig はそれについて
   何も言わない。

ゲートは `--self-test` を持ち、既知の不正ペアを 7 つ食わせて、それぞれが**それ自身の理由で**
拒否されることを要求する。拒否されただけでは足りない。検査 0 を追加したことで、既存の全ケースが
出所検査で拒否されうるようになった。「拒否されたか」だけを問う自己テストなら、実際には 1 つの検査しか
働かせていないのに 7 件の PASS を報告していただろう。失敗できないゲートは証拠ではなく、
違う理由で失敗するゲートも証拠ではない。

どの `mitigation.md` も、その対策が**解決しないこと**を述べる。そして次の演習はたいてい、
その限界の 1 つを攻める——EX-B01 のトークンは EX-L01 のリプレイに破られる。EX-B01 自身が
予告したとおりに。この連鎖が成立するのは、解説が正直だからである。

## 公開する前に

`SAFE_USE.md`、`SECURITY.md`、`ASSURANCE.md`、`docs/legal/export-control.md` があるのは、
このリポジトリが動く攻撃を配るからである。「EAR99」や「ITAR 非該当」とどこにも書かないこと。
何も分類されていない。実在の識別子・周波数・鍵・エンドポイントを何にも足さないこと。

## どこに何があるか

```
tools/renode-probe/probe.sh     証拠のゲート
tools/setup-toolchain.sh        Zephyr + SDK。root 不要、Docker 不要
firmware/common/                C 側のワイヤコーデック。Python 側とバイト単位で照合済み
firmware/apps/{comm,obc,eps,adcs}/  ノードの役割。ソースツリーは 1 つ、プロファイルごとに 1 ビルド
src/cuberange/proto/            CCSDS, PUS, CSP のコーデックとデフレーマ
src/cuberange/renode/           スーパーバイザ、Monitor クライアント、External Control、電源ドメイン
src/cuberange/gs/               地上局
src/cuberange/channel/          攻撃者が傍受し再送に使うリンクプロキシ
attacker/TcpCanInjector.cs      Renode が実行時にコンパイルする 90 行の C#。ソケットから生 CAN
src/cuberange/ports.py          ポート表。1 箇所にまとめ、衝突テスト付き
src/cuberange/paths.py          ビルドディレクトリ。チェックアウトごとに一意。make out が表示する
src/cuberange/safety/           Renode の全起動が入る、loopback だけの名前空間
src/cuberange/identity.py       誰が誰か。identity.cmake の写しで、一致をテストが検査する
src/cuberange/gs/schedule.py    TC 計画と、EX-G01 が攻めるインポータ
src/cuberange/gs/import_policy.py  EX-G01 の違いのすべてである 2 つのポリシー
firmware/common/identity.cmake  CUBERANGE_SAT_INDEX から導く CSP アドレスと SCID
scripts/multi-node/constellation.resc  2 宇宙機、2 ハブ、2 リンク、1 Monitor
scripts/profiles/               interactive と ci の実行プロファイル。シナリオはどちらかを include する
tools/config_diff_gate.py       ストローマン対策ゲート。自己テスト付き
firmware-matrix.yml             ゲートが検査する脆弱版／緩和版のペア
tests/e2e/test_determinism.py   規則 G1/G2。主張ではなく再現
exercises/EX-*/                 各 5 ファイル
tests/golden/                   独立オラクル由来のベクタ。自前のコーデック由来ではない
tools/oracles/                  そのオラクル自体——csp_oracle.c、tc_oracle.c、ビルドスクリプト
docs/superpowers/specs/         設計。3.2 節が欠陥、16 節が訂正
```
