# 貢献の手引き

*English: [CONTRIBUTING.md](CONTRIBUTING.md)*

## このプロジェクト全体が従う規則

**実行して確かめていないことを主張しない。**

このプロジェクトが依存する能力には、リポジトリ内のコマンドが必ず対応する。
そのコマンドを走らせるハーネスは、わざと失敗する自己テストを内蔵している。

設計仕様書の最初の2版は、一度も実行されていない Renode の挙動を「検証済み」と称する節を丸ごと持っていた。
実行したところ、6件の主張が一度に反証された。
MCU の選定もその一つである。
設計仕様書の16節がその記録である。
規則が存在する理由はそこにある。

実務としては、主張を足すならコマンドを足す。
テストを足すなら、それが失敗できることを示す。

## 演習を寄稿する

演習は5つのファイルからなり、5つとも必須である。

```
exercises/EX-XXX-short-name/
  README.md          シナリオ、目的、3段階のヒント、何を結論すべきか
  scenario.resc      Renode の構成
  solve.py           模範解答
  mitigation.md      対策と、それが解決しないこと
  verify_ex_xxx.py   検証。ファイル名が一意であることに注意
```

`verify_ex_xxx.py` は1つではなく**3つ**を assert しなければならない。

1. 脆弱版に対して攻撃が成功する
2. 対策がそれを止める
3. **対策版でも正規の操作が実行できる**

3番目は任意ではない。
運用者まで止めてしまう「対策」は障害であり、この assert が無ければそれが合格してしまう。

テストファイルの basename は演習をまたいで一意でなければならない。
`verify_test.py` という名前のファイルが2つあると同時に収集できず、その失敗はビルド出力の中によく紛れる。

### 脆弱版と対策版はただ1点だけ違うこと

欠陥はビルドフラグ1個の背後に置き、他は何も変えない。
ボードも `prj.conf` もコンパイラオプションも動かさない。

以前はこれを手で確かめるよう求めていた。
今は `tools/config_diff_gate.py` が `firmware-matrix.yml` に宣言された全ペアについて機械的に証明し、`make check` の一部になっている。

```bash
OUT=/tmp/cuberange python3 tools/config_diff_gate.py
```

5つを検査する。

1. 2つのビルドの Kconfig 出力が完全に一致すること
2. 宣言したもの以外の `CUBERANGE_*` キャッシュ変数が動いていないこと
3. 2つの ELF がバイト同一でないこと
4. フラグがアプリケーションのソースから実際に参照されていること
5. すべての翻訳単位が、宣言した `-D` を除いて同一のコマンドラインでコンパイルされていること

3から5が必要なのは、1と2が両方通ってもフラグが何もしていない場合があるからである。
キャッシュ変数を綴り間違えると、同一のイメージが2つできて Kconfig の差分は完璧に空になる。
そのペアは、一度もコンパイルされなかった対策を「証明」することになる。

5番目は Kconfig では見えない穴を塞ぐ。
Kconfig はコンパイラオプションについて何も語らないので、脆弱側だけ `-fno-stack-protector` を付けたビルドは1から4を通ってしまう。

これが何かを出力したら、その演習はプラットフォームを弱めて作った脆弱性であり、真でないことを教えている。
防御を切らないと成立しない演習は、その旨を README にその言葉で書き、学習者が代わりに何を結論すべきかを説明すること。

### 標的は合成物であること

実在の識別子、周波数、鍵、軌道要素、エンドポイントを置かない。
[SAFE_USE.ja.md](SAFE_USE.ja.md) を見ること。

### 外部通信をしないこと

演習はネットワークに到達してはならない。
固定されたビルド成果物の取得は別の工程で行い、演習の実行中は何もダウンロードしない。

### `mitigation.md` は対策が解決しないことを書くこと

このリポジトリのどの対策も自らの限界を列挙しており、次の演習はたいていそのうちの一つを突く。
EX-B01 のトークンは EX-L01 のリプレイに破られる。
EX-B01 の対策文書がそう予告したとおりである。
この連鎖が成立するのは、書き方が正直だからにほかならない。
売り込みすぎた対策は、対策が無いより悪い。

## コードを寄稿する

- ホスト側の Python と共有 C コーデックは、攻撃者が制御するバイト列を解析する。これらは脆弱であってはならない。`tests/native` はコーデックを `-fsanitize=address,undefined` でビルドする。汚さないこと。

- C と Python のコーデックは同じワイヤ形式の別実装であり、`tests/pytest/test_c_matches_python.py` がバイト単位で突き合わせる。同じ誤りを共有する自作コーデック2つは互いを完璧に検証してしまう。`tests/golden/` の独立オラクルと相互チェックが存在するのはそのためである。どちらも弱めないこと。

- 固定されたバージョンには理由がある。Renode、Zephyr、CSP のバージョン、エミュレーションの量子を変える前に、引き継ぎ文書の5節を読むこと。

- `make check` を走らせること。最後の行が `CHECK PASSED` でなければならない。明示的にそう書いてあるのは、失敗したサブターゲットが 8500 行の出力の中に一度隠れたからである。

## レビュー

exploit のコード、暗号、CI ワークフロー、リリース自動化への変更には、2人分の目が欲しい。
正式な当番制は無い。
2人目の読み手がいなかった場合は、そう pull request に書くこと。
いたと誰かが思い込まないように。

不透明なバイナリ、ベンダリングされた blob、実行時にダウンロードされるものは却下する。

## 出自

コードの出どころを書くこと。
`src/cuberange/renode/extctl.py` は Renode の MIT ライセンスのクライアントから逆解析したプロトコルを実装している。
`tests/golden/csp.json` は libcsp（MIT）を実行した出力である。
どちらも自身のヘッダにそう書いてあり、同種のものも同じようにすること。
